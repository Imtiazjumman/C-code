#include <stdio.h>
#include <stdlib.h>
//control statement -> Conditional and loop control statement......
// conditional control statement ( if-else , Switch)
// relational operators ( ==, > < , >= =<, ! != )

int main()
{
    /*
    // EVEN, ODD.........
        int num1;
        printf("Enter the number: ");
        scanf("%d",&num1);

        if(num1%2 == 0)
        {
            printf("The Number is 'EVEN'\n");
        }

        if(num1%2 != 0)
        {
            printf("The Number is 'ODD'\n");
        }

        else
        {

            printf("The Number is 'ODD'\n");
        }

    */
    /*
    // Find the large number between two values............
        int num1,num2;

        printf("Take Two Input: ");

        scanf("%d %d",&num1,&num2);

        if(num1>num2)
        {
            printf("The Large num is : %d",num1);
        }

        else if(num2>num1)
        {
            printf("The Large num is : %d",num2);
        }

         else
        {
            printf("The Number are EQUAL");
        }
    */

    /*
    //Letter Grade using if-else if-else......

        float marks;
        printf("Enter Marks: ");
        scanf("%f",&marks);

        if(marks >= 80)
        {
            printf("A+");
        }

        else if(marks >= 70)
        {
            printf("A");
        }

        else if(marks >= 60)
        {
            printf("A-");
        }

        else if(marks >= 50)
        {
            printf("B");
        }

        else if(marks >= 40)
        {
            printf("C");
        }
        else
        {
            printf("Fail");
        }

    */

    /*
    // Positive/Negative ............

        int num;
        printf("Take an Integer Number : ");
        scanf("%d",&num);

        if (num>0)
        {
            printf("\nPositive\n");
        }

        else if (num<0)
        {
            printf("\nNegative\n");
        }

        else
        {
            printf("\nZERO\n");
        }

     */

    return 0;
}
